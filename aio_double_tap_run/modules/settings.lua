return {
    normal_speed = 1.0,  -- Default speed when not sprinting.
    sprint_speed = 1.5,  -- Speed when sprinting.
    -- Double tap configuration
    double_tap_run = {
        interval       = 0.5,  -- Time window for double tap (seconds)
        liquid_update_interval = 0.5,  -- Time window for liquid check (seconds)
    }

}